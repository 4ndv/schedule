<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Schedule</title>

    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag bootstrap_css %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <%= javascript_include_tag bootstrap_js %>

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Навигация</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">Расписание</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <div class="nav navbar-nav">
            <li><%= link_to 'Институты', institutes_path %></li>
            <li><%= link_to 'Преподаватели', teachers_path %></li>
            <li><%= link_to 'Предметы', lessons_path %></li>
          </div>
        </div>
      </div>
    </nav>

    <% if current_page?(root_path) %>
      <div class="jumbotron">
        <div class="container">
          <h1>Расписание</h1>
          <p>Сайт, на котором можно просмотреть расписание.</p>
        </div>
      </div>
    <% end %>

    <div class="container">
      <% if flash[:notice] %>
        <br />
        <div class="row">
          <% if flash[:notice].is_a?(Array) %>
            <% flash[:notice].each do |item| %>
              <%= alert_box item, dismissible: true %>
            <% end %>
          <% else %>
            <%= alert_box flash[:notice], dismissible: true %>
          <% end %>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <br />
        <div class="row">
          <% if flash[:alert].is_a?(Array) %>
            <% flash[:alert].each do |item| %>
              <%= alert_box item, dismissible: true, context: :warning %>
            <% end %>
          <% else %>
            <%= alert_box flash[:alert], dismissible: true, context: :warning %>
          <% end %>
        </div>
      <% end %>
      <%= yield %>  
    </div>

    <div class="container">
      <hr>

      <footer>
        <p>&copy; 2016 Andrey Viktorov</p>
      </footer>
    </div>
  </body>
</html>
